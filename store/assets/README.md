# Upkopp release artwork

- `promo-440x280.png`: 440×280 RGB PNG promotional tile, using Upkopp's supplied symbol and bundled Poppins. Ready for owner review and upload as a promotional image.
- `promo-440x280.svg`: editable vector counterpart. Text appearance depends on available fonts; the PNG has the intended Poppins rendering.
- `github-cover.svg`: editable 1280×640 repository cover, used in README.md. It describes the product purpose without claiming store availability.
- The 128×128 extension icon is `../../assets/icon128.png`.

The document illustrations are brand graphics, not screenshots of the extension. Store screenshots are a separate, pending release task: capture the actual final extension at 1280×800 or 640×400 with public/sample content.

Suggested screenshot sequence: Content with original paragraphs; Page map with a selected heading; completed Content + Page map export workflow; Images with selection and ZIP progress; Settings and local processing explanation. Only show implemented behavior. Keep screenshots full bleed and readable at half size.
